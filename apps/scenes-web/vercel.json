{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=eav-scenes-web",
  "installCommand": "cd ../.. && corepack enable && pnpm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
